#============================+++++++ Внешний цикл +++++++==================================

# for outer in range(1, 4) :
#     print(f"🚀 Внешний цикл №{outer} начался")
#
#     # Внутренний цикл
#     for inner in range(1, 4) :
#         print(f"   🎯 Внутренний цикл №{inner} (внешний цикл №{outer})")
#
#     print(f"✅ Внешний цикл №{outer} закончился\n")

#<><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>
# print("📌 Таблица умножения\n")
#
# for i in range(1, 5):  # внешний цикл от 1 до 5
#     for j in range(1, 5):  # внутренний цикл от 1 до 5
#         print(f"{i} × {j} = {i * j}")
#     print("\n─── Конец строки ───\n")  # разделение строк таблицы
'''
Начало программы
       │
       ▼
i = 1 (берём первый элемент внешнего цикла)
       │
       ▼
  j = 1 (берём первый элемент внутреннего цикла)
       │
       ▼
Выводим результат i × j
       │
       ▼
j увеличиваем на 1 ◀───────┐
       │                    │
Проверяем: j ≤ 5? ─── Да ───┘
       │
      Нет
       │
       ▼
Выводим разделитель строки (конец строки)
       │
       ▼
i увеличиваем на 1 ◀────────┐
       │                    │
Проверяем: i ≤ 5? ─── Да ───┘
       │
      Нет
       │
       ▼
  Конец программы

🎯 Короткий итог (чтобы легче запомнить):
Внешний цикл (i) задаёт строки таблицы (первое число умножения).

Внутренний цикл (j) задаёт столбцы (второе число умножения).

Внутренний цикл всегда выполняется полностью на каждом шаге внешнего цикла.

Это аналогично тому, как ты заполняешь таблицу:
сначала проходишься по первой строке (цикл по столбцам),
затем переходишь к следующей строке (цикл по строкам).
'''
#<><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>
# rows = 10  # высота пирамиды
#
# for i in range(1, rows + 1):         # внешний цикл (строки пирамиды)
#     for j in range(i):               # внутренний цикл (звездочки в строке)
#         print("*", end=" ")          # вывод звёздочки в строке без переноса строки
#     print()                          # переход на новую строку после каждого ряда
#<><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>
# hour = 0  # начальное значение часов
#
# while hour < 3:  # внешний цикл (часы)
#     print(f"⏰ Час: {hour}")
#
#     minute = 0  # начальное значение минут для каждого часа
#     while minute < 5:  # внутренний цикл (минуты)
#         print(f"   🕑 Минута: {minute}")
#         minute += 1  # увеличиваем минуты
#
#     print("------ Следующий час ------\n")
#     hour += 1  # увеличиваем часы

'''
🚀 Как экспериментировать с этим кодом:
Измени значения циклов (например, добавь секунды).
Попробуй заменить внутренний цикл на for, чтобы сравнить поведение циклов.
Попробуй добавить условие (if), чтобы пропустить какие-то минуты или часы.
'''
# hour = 0
#
# while hour < 3:  # внешний цикл (часы)
#     print(f"⏰ Час: {hour}")
#
#     minute = 0
#     while minute < 5:  # второй уровень вложенности (минуты)
#         print(f"   🕑 Минута: {minute}")
#
#         second = 0
#         while second < 5:  # третий уровень вложенности (секунды)
#             print(f"      ⏱️ Секунда: {second}")
#             second += 1  # увеличиваем секунды
#
#         minute += 1  # увеличиваем минуты
#
#     print("------ Следующий час ------\n")
#     hour += 1  # увеличиваем часы
'''
📍 Что именно я изменил (ключевые моменты):
Цикл секунд теперь находится внутри цикла минут.

Корректно выставил отступы (4 пробела на каждый уровень вложенности).

📌 Принцип вложенности циклов для твоего примера (таблица аналогий):
Уровень вложенности	Что это в жизни?
1 (внешний цикл)	⏰ Часы
2 (средний цикл)	🕑 Минуты
3 (внутренний цикл)	⏱️ Секунды
Иерархия циклов — это ключ к пониманию вложенных циклов:

Сначала завершается внутренний цикл (секунды),

потом средний (минуты),

потом внешний (часы).
'''
# for i in range(4):  # внешний цикл (часы)
#     print(f"⏰ Час: {i}")
#
#     for j in range(4):  # внутренний цикл (минуты)
#         print(f"   🕑 Минута: {j}")
#
#     print("------ Следующий час ------\n")
#<><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>
# products = ["яблоко", "банан", "чипсы", "хлеб", "пепси"]
# bad_items = ["чипсы", "пепси"]
#
# for item in products:
#     is_bad = False
#
#     for bad in bad_items:
#         if bad == item:
#             is_bad = True
#             break
#
#     if is_bad:
#         continue
#
#     print(f"✔ Добавлено в корзину: {item}")

'''
🧠 Что «говорит» Python во время выполнения:
🔁 Первая итерация:
🧠 Беру item = "яблоко"
➕ Ставлю is_bad = False
🔍 Проверяю bad = "чипсы" → не совпадает
🔍 Проверяю bad = "пепси" → не совпадает
🧪 is_bad == False → не сработало
✅ Печатаю: ✔ Добавлено в корзину: яблоко
🔄 Перехожу к следующему item

🔁 Вторая итерация:
🧠 Беру item = "банан"
➕ is_bad = False
🔍 bad = "чипсы" → не совпадает
🔍 bad = "пепси" → не совпадает
🧪 is_bad == False
✅ Печатаю: ✔ Добавлено в корзину: банан
🔄 Дальше

🔁 Третья итерация:
🧠 Беру item = "чипсы"
➕ is_bad = False
🔍 bad = "чипсы" → О! Совпадает!
⚠ Ставлю is_bad = True
⛔ break → выхожу из вложенного цикла
🔎 Проверка if is_bad → Да, True
🚫 continue → НЕ ПЕЧАТАЮ НИЧЕГО, сразу к следующему item

🔁 Четвёртая итерация:
item = "хлеб"
is_bad = False
bad = "чипсы" → не совпадает
bad = "пепси" → не совпадает
is_bad == False
✅ ✔ Добавлено в корзину: хлеб

🔁 Пятая итерация:
item = "пепси"
is_bad = False
bad = "чипсы" → не совпадает
bad = "пепси" → Совпадает!
is_bad = True
break
if is_bad: → True
continue → не печатаю, перехожу к следующему (которого уже нет)
'''
#<><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><><>